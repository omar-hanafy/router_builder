name: route_generator
description: "route generator for devs"
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.7
  source_gen: ^2.0.0
  build: ^2.5.4
  analyzer: ^7.4.5
  glob: ^2.1.3
  dart_helper_utils: ^5.2.0
  dart_style: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^9.0.0
  build_runner: ^2.5.4
